apiVersion: v1
kind: pod
metadata:
   name: flask-pod
   labels:
      app: flask-demo
      environment: demo

spec:
   container:
      - name: flask-container
        image: jack17kdb/flask-demo:latest
        ports:
          - containerPort: 5000
