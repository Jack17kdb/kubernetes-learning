apiVersion: v1
kind: Service
metadata:
   name: rex-service
   labels:
      app: rex-flask
      environment: rex

spec:
   selector:
      app: rex-flask
   ports:
      - protocol: TCP
        port: 80
        targetPort: 5000
   type: ClusterIP
